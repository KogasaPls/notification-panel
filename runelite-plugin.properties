displayName=Notification Panel
author=Kogasa
support=https://github.com/KogasaPls/notification-panel
description=Displays notifications in a movable overlay panel
# The Hub search and the installed-plugin search both index these, so a plugin with none is
# findable only by its exact name.
tags=notification,notifications,alert,alerts,popup,toast,overlay,panel,rules,filter,color,colour
plugins=com.notificationpanel.NotificationPanelPlugin
# Standard mode replaces build.gradle and settings.gradle at submission time, so the Hub reads the
# published version from here rather than from the Gradle project.
version=2.0.0
build=standard
