# The build pins a Java 11 toolchain, which Gradle locates by scanning common install
# directories. GitHub's setup-java action installs JDKs into the runner tool cache instead,
# so point detection at the environment variables it exports. Ignored when they are absent.
org.gradle.java.installations.fromEnv=JAVA_HOME_11_X64,JAVA_HOME_11_ARM64
